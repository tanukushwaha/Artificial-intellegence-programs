#define a futnction that computes length of the string
def islist(lis):
    cn=0
    for i in lis:
        cn+=1


str=input("Enter a string for computing the length of the string or for a list: ")
list1=[]
while(list1<str):
    list1+=1
    print(list)
print("Length of the entere string is: ",len(str))
print("Length of the entered list is:",islist(str))
